#include <stdio.h>

void recursion (int N){
    if(N){
        printf("%d ", N);
        recursion(N-1);
    }
}
    
int main() {
    int N;
    printf("Enter value of N:\n");
    scanf("%d",&N);
    recursion(N);
    return 0;
}
